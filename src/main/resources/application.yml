spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/find_pill?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1879
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: 'true'
        format_sql: 'true'
